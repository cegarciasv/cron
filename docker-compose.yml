version: "3.9"
services:
  php-test:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./public:/var/www/html:rw
    restart: unless-stopped
    # Si necesitas variables de entorno para PHP (ej. para bases de datos)
    # environment:
    #   MYSQL_HOST: db
    #   MYSQL_USER: user
    #   MYSQL_PASSWORD: password
    #   MYSQL_DATABASE: mydatabase
    # depends_on:
    #   - db # Si tuvieras un servicio de base de datos llamado 'db'

  # Ejemplo de cómo añadir un servicio de MySQL (descomentar y configurar si es necesario)
  # db:
  #   image: mysql:8.0
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root_password
  #     MYSQL_DATABASE: mydatabase
  #     MYSQL_USER: user
  #     MYSQL_PASSWORD: password
  #   volumes:
  #     - db_data:/var/lib/mysql

# volúmenes nombrados para persistencia de datos (descomentar si se usa 'db')
# volumes:
#   db_data: